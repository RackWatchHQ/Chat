{
  "name": "rackwatch",
  "version": "0.1.0",
  "private": true,
  "description": "RackWatch network monitoring — reasoning engine, scheduler, persistence, and WebSocket server",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "net-snmp": "^3.26.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/net-snmp": "^3.23.0",
    "@types/node": "^22.15.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
